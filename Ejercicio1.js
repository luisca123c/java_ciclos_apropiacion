// Iniciamos un ciclo que va del 1 al 20
for(let i = 1 ; i <= 20; i++) {
    // Verificamos si el número es múltiplo de 3
    if(i % 3 == 0) {
        // Mostramos una alerta si es múltiplo de 3
        alert(`${i} es múltiplo de 3`);
    }
}
